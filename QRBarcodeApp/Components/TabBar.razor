<nav class="flex justify-between fixed bottom-0 w-full h-[50px] bg-[#141414]">
    @foreach (string tabName in new[] { "scan", "generate", "favorites", "history", "settings" })
    {
        <TabBarNavLink Href=@($"/{tabName}") IconSrc=@($"{tabName}.svg") />
    }
</nav>
